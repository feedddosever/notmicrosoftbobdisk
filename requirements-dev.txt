# Development and tooling dependencies. openpyxl and reportlab are dev/tool-only
# (workbook generation, mapping, manual PDF) and are never imported by the service.
# pypdf (BSD) is used only by the manual text check in tools/make_manual.py.
-r requirements.txt
pytest>=8,<10
openpyxl>=3.1,<3.2
httpx>=0.27,<1
pip-licenses>=5,<6
pyyaml>=6,<7
reportlab>=4,<6
pypdf>=4,<7
# uvicorn (BSD-3) runs the API locally (README quick start); Vercel and the Dockerfile bring their own server.
uvicorn>=0.30,<1
